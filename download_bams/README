These are the scripts used to download the bams. 

The file that starts with "ena" is the ENA generated set of scripts to download the bams. 

The files that start with "slurm" have the slurm header. 

The file that ends with "-download-missed-files.sh" is to download the files that were missed due to the first script timing out. 
